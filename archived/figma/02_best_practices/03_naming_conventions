# Naming Conventions

- **Components:** Pascal case
- **Styles etc.:** Title case with spaces in between.<br>
- `Btn`: Button
- `Bg`: Background
- `* Wrapper`: Use 'wrapper' sparingly. This is used to indicate a container within which another/other container/s is/are directly placed but layout settings are made e.g. padding etc. within the first container.
