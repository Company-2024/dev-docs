# Dependency Management

## Pip

### `requirements.txt`

This should also specify version numbers. It should also be limited to only the
packages directly installed not the packages' dependencies. As such, do not use
`pip freeze` to generate the `requirements.txt` file.
